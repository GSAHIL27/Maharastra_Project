# Maharashtra Company Registration Analysis (2015)

This project analyzes **company registration data** for Maharashtra (2015) and provides insights such as:
- District-wise registration counts
- Authorized capital categorization
- Visual plots for top districts and capital categories

## Installation

1. **Clone the repository:**
   ```bash
   git clone <repo-url>
   cd Maharastra_Project

## Create Virtual Enviornment

python3 -m venv venv
source venv/bin/activate   # For Linux/macOS
venv\Scripts\activate      # For Windows

## Install dependencies

pip install -r requirements.txt


The project uses the following Python libraries:

matplotlib
collections
csv

